import Page from "../../containers/Page";

const OffererList = (): JSX.Element => {
  return (
    <Page title="Workers available">
      <p>These are all the workers available.</p>
    </Page>
  );
};

export default OffererList;
